pages:
  image: node:alpine
  stage: deploy
  cache:
    key: "2017-12-02"
    paths:
      - node_modules/
  script:
    - npm install
    - npm run build
    - mkdir public
    - mv dist/* public
  artifacts:
    paths:
      - public
  only:
  - master
